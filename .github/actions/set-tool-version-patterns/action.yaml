name: Set matrix of tool versions to be published

runs:
  using: 'composite'
  steps:
  - name: Set matrix of tool versions to be published
    uses: mikefarah/yq@master
    id: set-matrix
    with:
      cmd: yq -o json ./matrix.yaml
