{
  "name": "@awesome-backup/storage-service-clients",
  "version": "0.1.0",
  "description": "Storage service clients module of awesome-backup",
  "author": "Ryu Sato <ryu@weseek.co.jp>",
  "license": "MIT",
  "homepage": "https://github.com/ryu-sato/awesome-backup#readme",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/ryu-sato/awesome-backup.git"
  },
  "bugs": {
    "url": "https://github.com/ryu-sato/awesome-backup/issues"
  },
  "// Use Typescript as is, so Typescript must be enabled": "",
  "main": "src/index.ts",
  "directories": {
    "test": "__tests__"
  },
  "files": ["dist"],
  "scripts": {
    "build": "tsc && tsc-alias",
    "// Since the risk of memory leaks increases when a few workers, we chose to use many workers even though the processing speed would be lower.": "",
    "test": "cross-env NODE_ENV=test jest --maxWorkers 4 --ci --logHeapUsage",
    "lint": "eslint src --ext .ts",
    "lint:fix": "eslint src --ext .ts --fix"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.85.0",
    "@google-cloud/storage": "^5.19.4",
    "axios": "^0.27.2",
    "axios-retry": "^3.2.5",
    "cross-env": "^7.0.3",
    "bunyan": "^1.8.15",
    "universal-bunyan": "^0.9.2",
    "browser-bunyan": "^1.6.3",
    "date-fns": "^2.28.0",
    "node-schedule": "^2.1.0",
    "tmp": "^0.2.1"
  },
  "devDependencies": {
    "@awesome-backup/tsconfig": "^0.1.0",
    "@types/bunyan": "^1.8.8"
  },
  "engines": {
    "node": ">= 16.13.1 < 17",
    "yarn": ">= 1.22.4"
  }
}
